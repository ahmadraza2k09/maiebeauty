# npm registry configuration for Vercel
# This optimizes npm install on Vercel

# Use npm registry
registry=https://registry.npmjs.org/

# Production mode on Vercel (don't install devDependencies in production)
# Vercel automatically sets NODE_ENV=production during build

# Install dependencies as npm ci for deterministic installs
# Vercel uses package-lock.json for reproducible builds
